name: Example Workflow
on:
  push:
    branches:
      - main

jobs:
  example-job:
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v2

      - name: uses secret in script
        run: echo "the secret is ${{ secrets.MY_SECRET}}"
